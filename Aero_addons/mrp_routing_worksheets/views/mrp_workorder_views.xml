<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="mrp_production_workorder_form_view_inherit" model="ir.ui.view">
            <field name="name">mrp.workorder.form</field>
            <field name="model">mrp.workorder</field>
            <field name="inherit_id" ref="mrp.mrp_production_workorder_form_view_inherit"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='company_id']" position="after">
                    <field name="worksheet_ids_count" invisible="1"/>
                </xpath>
                <xpath expr="//page[last()]" position="after">
                    <page name="worksheets" string="Liste documents" attrs="{'invisible':[('worksheet_ids_count', '==', 0)]}">
                        <field name="worksheet_ids">
                            <tree string="Work Instructions">
                                <field name="name"/>
                                <field name="type"/>
                                <field name="code"/>
                                <field name="validation"/>
                                <field name="worksheet_type"/>
                            </tree>
                            <form string="Work Instructions">
                                <group>
                                    <field name="name"/>
                                    <field name="type"/>
                                    <field name="code"/>
                                    <field name="validation"/>
                                    <field name="worksheet_type" widget="radio"/>
                                    <field name="worksheet" help="Upload your PDF file." widget="pdf_viewer" attrs="{'invisible':  [('worksheet_type', '!=', 'pdf')], 'required':  [('worksheet_type', '=', 'pdf')]}"/>
                                    <field name="worksheet_google_slide" placeholder="Google Slide Link" widget="embed_viewer" attrs="{'invisible':  [('worksheet_type', '!=', 'google_slide')], 'required': [('worksheet_type', '=', 'google_slide')]}"/>
                                    <field name="note" attrs="{'invisible':  [('worksheet_type', '!=', 'text')]}"/>
                                </group>
                            </form>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
